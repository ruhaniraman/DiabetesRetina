# Used by `npm run build`. In production the web app and both APIs share one https origin behind the reverse
# proxy (see deploy/Caddyfile), so the API addresses are relative paths, not localhost ports.
VITE_AUTH_API_URL=/auth-api
VITE_ML_API_URL=/ml-api
