# Core
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pydantic>=2
httpx>=0.27
python-dotenv>=1.0

# Image processing
numpy
opencv-python-headless

# Downloadable PDF report
reportlab>=4

# Offline translation (Hindi / Kannada)
argostranslate

# Stage 3/4 need MATLAB's Python engine, which is NOT on PyPI under this name.
# Install it from your MATLAB install:
#   cd "<matlabroot>/extern/engines/python" && python -m pip install .
# (or 'pip install matlabengine==<version matching your MATLAB release>')
